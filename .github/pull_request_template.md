## What and why

<!-- What changes, and what problem it solves. Link an issue if there is one. -->

## Checklist

- [ ] `uv run ruff check --fix` and `uv run ruff format`
- [ ] `uv run ty check`
- [ ] `uv run pytest` — passes, coverage still ≥ 90%
- [ ] A bug fix has a test that failed before the fix
- [ ] README and CHANGELOG updated if behaviour or settings changed

## Measurements

<!-- Required for changes to transcription speed or quality: the
     scripts/bench_transcribe.py table, before and after. Delete if not applicable. -->
